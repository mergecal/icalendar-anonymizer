# SPDX-FileCopyrightText: 2025 icalendar-anonymizer contributors
# SPDX-License-Identifier: AGPL-3.0-or-later

name: Create Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Create Release
        uses: ncipollo/release-action@v1
        with:
          generateReleaseNotes: false
          body: |
            ## Installation

            **PyPI:**
            ```bash
            pip install icalendar-anonymizer==${{ github.ref_name }}
            ```

            **Docker:**
            ```bash
            docker pull sashankbhamidi/icalendar-anonymizer:${{ github.ref_name }}
            ```

            ## Documentation

            https://icalendar-anonymizer.readthedocs.io/

            ## Changelog

            https://icalendar-anonymizer.readthedocs.io/en/latest/changelog.html
